import "./game-board.scss";

import React from "react";
import {ContainerBase} from "../../lib/component";

export default class GameBoard extends ContainerBase {
    render(){
        return (
            <section className="c-game-board">GAME BOARD</section>
        );
    }
}
